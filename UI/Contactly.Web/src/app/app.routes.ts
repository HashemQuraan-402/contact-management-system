import { Routes } from '@angular/router';
import { ContactsInformation } from './components/contacts-information/contacts-information';

export const routes: Routes = [
    
    {path:'',component:ContactsInformation},
];
